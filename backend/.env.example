# Server Configuration
NODE_ENV=development
PORT=5000

# Database
DATABASE_URL="postgresql://username:password@localhost:5432/student_finance_db?schema=public"

# JWT Secrets - CRITICAL SECURITY CONFIGURATION
# ⚠️  NEVER use these placeholder values in production!
# ⚠️  Generate secure secrets using: node scripts/generate-secrets.js
# Or use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#
# Requirements:
# - Minimum 64 characters (256-bit)
# - Cryptographically random
# - Different for each environment
# - Rotate every 90 days in production
JWT_SECRET=REPLACE_WITH_64_CHAR_RANDOM_STRING_FROM_GENERATE_SECRETS_SCRIPT
JWT_REFRESH_SECRET=REPLACE_WITH_DIFFERENT_64_CHAR_RANDOM_STRING
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# CORS
FRONTEND_URL=http://localhost:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Redis (for token blacklist and AI cache)
# Get free Redis from https://upstash.com or use local Redis
REDIS_URL=redis://localhost:6379

# Account Lockout Settings
LOCKOUT_THRESHOLD=5
LOCKOUT_DURATION_MINUTES=15

# SMTP Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
EMAIL_FROM="Student Finance" <noreply@studentfinance.com>

# Hugging Face API (for AI categorization - free)
# Get your key at https://huggingface.co/settings/tokens
# HUGGING_FACE_API_KEY=hf_your_token_here

# Production Secrets Management:
# - Never commit .env file to version control
# - Use a secrets manager (AWS Secrets Manager, Google Secret Manager, etc.)
# - Set secrets via environment variables in production
# - Rotate secrets regularly
