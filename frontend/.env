VITE_API_URL=http://localhost:5000

# ⚠️ SECURITY WARNING:
# NEVER put secrets (JWT_SECRET, API keys, etc.) in frontend .env files
# Frontend .env files are bundled into the JavaScript and exposed to users
# Only put non-sensitive configuration here (API URLs, feature flags, etc.)
#
# Secrets belong ONLY in backend/.env
